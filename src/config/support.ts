/* How students reach AfaqWay.
 *
 * One number, one place. It was written directly into the contact page, and the
 * Journey needed the same number for the end-of-journey voice message — which is
 * exactly the moment a second copy starts drifting from the first.
 */

/** Digits only, international format, as wa.me expects. */
export const SUPPORT_WHATSAPP = "212632501155";

/** The wa.me address with no message attached. */
export const SUPPORT_WHATSAPP_URL = `https://wa.me/${SUPPORT_WHATSAPP}`;
