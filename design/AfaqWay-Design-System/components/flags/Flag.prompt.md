One flag component, three sizes. Prefer a real asset via src; stripes is the documented stopgap; emoji only inside native <option>.

```jsx
<Flag size="lg" stripes={['#FDB913','#006A44','#C1272D']} selected />
<Flag size="md" emoji="🇩🇪" unavailable />
```