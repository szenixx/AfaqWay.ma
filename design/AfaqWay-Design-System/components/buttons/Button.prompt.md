The platform's only button. Labels are single active verbs: Continue, Upload, Save draft, Withdraw.

```jsx
<Button variant="primary" size="lg">Continue</Button>
<Button variant="ghost">Back</Button>
<Button variant="neutral">Cancel</Button>
<Button variant="destructive">Withdraw application</Button>
```

Radius 12px, height 40/44, label 14-15/600. Hover indigo-500, pressed indigo-700, disabled 40% opacity. Pass an 18px <Icon> via the icon prop.