Inline status pill for lists and tables. Label defaults to the canonical word for the state.

```jsx
<StatusPill status="under-review" />
```

Never invent status synonyms — the five canonical words only.