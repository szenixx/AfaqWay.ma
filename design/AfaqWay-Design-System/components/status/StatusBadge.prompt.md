Circle-housed status marker for document rows, application steps, checklists. Never show a bare unhoused glyph for a state.

```jsx
<StatusBadge status="approved" />
<StatusBadge status="under-review" label="Passport scan" caption="Uploaded 12 May" />
```

Five states only: not-started, applied, under-review, needs-changes, approved.