The entire surface language: one radius, one border, one whisper shadow.

```jsx
<Card eyebrow="Personal details" title="Tell us about you">…fields…</Card>
```

Also exports <Divider /> (1px line-soft) for separating content INSIDE a card instead of nesting.